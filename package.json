{
  "name": "purescript-sourcemap-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "build": "pulp build -r ../ --source-maps",
    "build-all": "npm run build && npm run webpack && npm run browserify",
    "webpack": "webpack -c webpack.config.js",
    "browserify": "browserify --debug .browserify.js -o browserify.js && sorcery -i browserify.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "source-map-loader": "^0.1.5"
  }
}
