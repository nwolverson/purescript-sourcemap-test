{
  "name": "purescript-sourcemap-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "build": "pulp build -- --source-maps",
    "build-all": "npm run build && npm run webpack && npm run browserify && npm run bundle",
    "webpack": "webpack",
    "browserify": "browserify --debug .browserify.js -o browserify.js && sorcery -i browserify.js",
    "bundle": "psc-bundle 'output/*/*.js' -m Main --main Main -o bundle.js --source-maps && sorcery -i bundle.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "source-map-loader": "^0.1.5"
  }
}
